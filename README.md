# Binary Clock

[Configurable Binary Clock](https://jalensailin.github.io/binary-clock-js/)

## To run locally

Clone repository and run `npm run dev`. This will start a local server which you can mess around with. You can also run `npm run build` which will build the app into the `/dist` directory.
